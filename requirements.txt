numpy>=1.20
scipy>=1.7
pandas>=1.3
scikit-learn>=1.0
matplotlib>=3.4
h5py>=3.0
tqdm>=4.60

# Optional, needed only for scripts that evaluate or train neural checkpoints.
# tensorflow>=2.8
# torch>=1.10

# Not pip-installable: the TCHES20 loaders (`dataLoaders`) and the EstraNet
# modules (`data_utils`, `data_utils_ches25`, `transformer`) come from their
# upstream repositories; see "External dependencies" in README.md.
